const searchesRoutesDefault = '/search';
const searchesRoutes = {
  list: {
    url: `${searchesRoutesDefault}/list`
  },
  create: {
    url: `${searchesRoutesDefault}`
  },
  searchInRadius: {
    url: `${searchesRoutesDefault}/inraio`
  }
}

export default searchesRoutes;