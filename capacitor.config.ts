import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tcc-vamos-frontend',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
